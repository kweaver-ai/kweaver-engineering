# BKN_Creator 中文交接摘要模板

需求发现阶段只输出业务语言。PRD 正文末尾的 `BKN_Creator` 交接摘要，用于帮助后续建模理解业务，不承担机器可读 schema 职责。

## 按场景收敛摘要

> 五层表达必须逐项业务化。不要只写“概念模型层有哪些、关系层有哪些”，而要说明每个对象、关系、逻辑、治理点和 Agent 任务是什么、为什么需要、怎么做或谁负责。
> 禁止压缩写成 `对象：A、B；关系：A-B；逻辑：X`。第 15 节必须比正文场景收敛更完整，不能更抽象。
> 每项应能用业务语言追溯来源场景、业务规则、验收用例或输入材料，并明确确认状态；不要为了追溯而输出机器字段。`来源 / 验收映射` 可写成“来自 S1 业务规则 / TC-S1-03 / 会议纪要第 N 条”。

#### S1 <场景名称>

##### 概念模型层：需要识别的业务对象

> 本体建模门禁：对象必须是稳定业务实体或事实载体；需说明身份、生命周期、责任、关系或管理动作；不得把编码、字段口径、统计维度、余额、数量、指标、结果、明细、行、报表直接当对象。
> 核心对象优先门禁：先识别核心业务对象，再判断是否拆细分对象；粒度、状态、数量、口径、扣减项默认是属性、枚举、关系或计算口径，不能直接升格为对象。
> 规则与角色不对象化门禁：业务规则、口径、算法判断、建议结果、场景角色名默认不是对象；应先归入关系约束、逻辑、Action 草案、治理要求或已有对象的场景角色。

| 业务对象 | 是什么 | 为什么需要 | 边界 | 确认状态 | 来源 / 验收映射 |
|---|---|---|---|---|---|
|  |  |  |  | 已确认 / 建模候选 / 待下游判定 |  |

> 对象说明至少覆盖身份线索、生命周期、责任角色或管理动作之一；否则标为候选或待下游建模判定。

##### 关系层：需要表达的业务联系

| 业务联系 | 连接什么 | 业务含义 | 为什么需要 | 确认状态 | 来源 / 验收映射 |
|---|---|---|---|---|---|
|  |  |  |  | 已确认 / 建模候选 / 待下游判定 |  |

> 本体一致性门禁：关系层出现的核心名词，必须已在概念模型层逐项说明。
> 关系说明必要时补充方向、约束、有效期或适用范围；计算结果、诊断结论和报表行不直接作为关系端点。

##### 动力层：需要判断、计算或推进的业务逻辑

| 类型 | 业务逻辑 | 怎么做 / 怎么判断 | 输出什么 | 边界 | 确认状态 | 来源 / 验收映射 |
|---|---|---|---|---|---|---|
| 指标 / 算子 / 状态变化 / 行动候选 |  |  |  |  | 已确认 / 建模候选 / 待下游判定 |  |

> 本体一致性门禁：动力层输出的结果必须判定为业务对象、派生结果或状态；结果类内容不得默认当作对象。
> 动力层说明必须区分指标、算子、状态变化和行动候选；行动候选需说明触发、前置、影响对象和失败处理。

##### 治理层：需要控制的责任、权限与留痕

| 治理点 | 控制什么 | 谁负责 / 谁使用 | 为什么需要 | 确认状态 | 来源 / 验收映射 |
|---|---|---|---|---|---|
|  |  |  |  | 已确认 / 建模候选 / 待下游判定 |  |

> 普通查看、刷新、筛选、导出不默认进入治理层；只有涉及责任、权限、审批、留痕、敏感数据、拒绝自动化或人工接管时才保留。

##### Skill / Agent 应用层：可支撑的业务任务

| Agent 任务 | 帮谁 | 做什么 | 输出要求 | 不能自动做什么 / 风险边界 | 确认状态 | 来源 / 验收映射 |
|---|---|---|---|---|---|---|
|  |  |  |  |  | 已确认 / 建模候选 / 待下游判定 |  |

## 全局归并摘要

### 业务已确认内容

- 仅放入已被业务材料、规则或验收用例支撑的内容。
- 确认状态门禁：凡含“待确认 / 未确认 / 待对齐 / 是否 / 范围待定 / 范围待确认 / 候选”的句子，不得进入本节。

### 仍属建模候选

- 放入 AI 工程师根据 PRD 推导出的对象、关系、指标、算子、逻辑能力、状态变化或行动候选。

### 需下游建模阶段判定的问题

- 放入对象边界、关系表达、指标 / 算子 / 逻辑能力挂载、行动执行模式等后续建模问题。
